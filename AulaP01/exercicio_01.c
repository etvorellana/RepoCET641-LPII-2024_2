#include <stdlib.h>

int geraValor(int min, int max);
/* 
    Gera um valor inteiros x entre min e max
    min <= x <= max
*/

float geraValor1();
/* 
    Gera um valor real x entre 0 e 1
    0 <= x <= 1
*/

float geraValorF(float min, float max);
/* 
    Gera um valor real x entre min e max
    min <= x < max
*/

